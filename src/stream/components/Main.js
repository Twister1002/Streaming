import React, { Component } from "react";

export default class Stream extends Component {
    render() {
        return (
            <div id="stream-overlay">
                overlay
            </div>
        )
    }
}